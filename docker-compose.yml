version: '3.7'
services:
  php:
    image: ramageek/bcrud:8.2-fpm-nginx-npm
    container_name: laravel-app_php
    volumes:
      - .:/var/www/html/
    ports:
      - "8000:80"
    environment:
      SSL_MODE: "off"
      AUTORUN_ENABLED: false
